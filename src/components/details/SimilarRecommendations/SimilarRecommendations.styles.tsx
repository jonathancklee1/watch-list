import { Flex } from "@chakra-ui/react";
import styled from "styled-components";
export const StyledHeadingWrapper = styled(Flex)`
    gap: 4;
    align-items: center;
    width: 100%;
    margin-block: 1em;
    padding-inline: 1em;
`;
